#!/bin/dash
#
# http://github.com/MitchWeaver/bin
#
# mass convert to opus
#

go() {
    for i in "$@" ; do
        echo $i
        find . -name "*.$i" -exec bash -c \
            'ffmpeg -loglevel -8 -i "{}" -y -c:a \
            libopus -ab 128k "${0/.$i}.ogg" && \
            rm "{}"' {} \; &
    done
}

go ogg aac flac mp3 wav mp4 webm 
