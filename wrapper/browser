#!/bin/sh

case "$BROWSER" in
    *chrom*|iridium)
        "$BROWSER" --new-window &
        ;;
    surf)
        tabbed -d -c surf -e "$@" &
        ;;
    *)
        >&2 echo '?'
        exit 1
esac
