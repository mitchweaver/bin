#!/bin/sh
#
# http://github.com/mitchweaver/bin
#
# local backup to my pi via rsync
#

[ "$1" ] && f="$*" || f='files images music src books'

d='pi:/mnt'
cmd='rsync -rtvuh --progress --delete --partial'

for i in $f ; do
    printf '--- %s\n\n' ${i}:
    $cmd ~/$i/ $d/$i
done
